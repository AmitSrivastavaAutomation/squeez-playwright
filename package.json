{
  "name": "squeez-playwright",
  "version": "1.0.0",
  "description": "Playwright automation for Squeez admin panel",
  "main": "index.js",
  "scripts": {
    "test": "playwright test || npm run report",
    "test:headed": "playwright test --headed || npm run report",
    "test:sanity": "playwright test tests/sanity --headed || npm run report",
    "test:regression": "playwright test tests/regression || npm run report",
    "regenerate:session": "node config/global-setup.js",
    "allure:generate": "allure generate ./allure-results --clean",
    "allure:open": "allure open",
    "report": "npm run allure:generate && npm run allure:open"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "allure-playwright": "^3.3.1",
    "prettier": "^3.6.1"
  },
  "dependencies": {
    "@playwright/test": "^1.54.1",
    "dotenv": "^16.5.0",
    "playwright": "^1.54.1",
    "xlsx": "^0.18.5"
  }
}
